/**
 * Do not remove the noConflict wrapper.
 *
 * This wrapper allows the $ to still represent jQuery without interfering with any other JS libraries that may be included.
 */
jQuery(document).ready(function($) {

    console.log('hello');

});
